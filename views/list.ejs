<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List </title>
</head>

<body>
    <% if (kindOfDay === "Saturday" || kindOfDay === "Sunday"){ %>
    <h1 style="color: rgb(238, 148, 13)"><%= kindOfDay %> ToDo List</h1>
    <% }else{ %>
    <h1 style="color: rgb(45, 106, 185)"><%= kindOfDay %> ToDo List</h1>
    <% } %>
</body>

</html>